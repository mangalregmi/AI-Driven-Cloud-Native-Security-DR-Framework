spring:
  application:
    name: log-telemetry-ingestion-service

server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: health,info
streaming:
  service:
    url: ${STREAMING_SERVICE_URL:http://localhost:8082}
